`model/` contains the model architecture, diffusion process, sampling code, and knowledge-distillation framework.

`dataset/` contains the UAV, AUAV, and deviating-node CSI dataset files.

`mcl_crypto_benchmark/` contains the MCL cryptographic benchmark source code and build files.
